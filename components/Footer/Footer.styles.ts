import { styled } from '@mui/system';

export const StyledFooter = styled('footer')({
  display: 'flex',
  flex: '1',
  padding: '2rem 0',
  borderTop: '1px solid #92CED0',
  justifyContent: 'center',
  alignItems: 'center',
  fontWeight: '900',
});
